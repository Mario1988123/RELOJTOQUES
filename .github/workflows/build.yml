name: Build All

on:
  push:
    branches: [ main, 'claude/**' ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  # Android APK build deshabilitado (no se usa)
  # ESP32 firmware usa ESP-IDF, no Arduino CLI
  # La compilación debe hacerse localmente con ESP-IDF

  placeholder:
    name: Workflow Placeholder
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Info
      run: |
        echo "Este repositorio usa ESP-IDF para el firmware ESP32"
        echo "Compilar localmente con: idf.py build"
