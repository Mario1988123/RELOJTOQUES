name: Build ESP32 Firmware

on:
  push:
    branches: [ main, 'claude/**' ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-esp32-idf:
    name: Build ESP32-S3 Firmware (ESP-IDF)
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v4
      with:
        submodules: 'recursive'

    - name: ESP-IDF Build
      uses: espressif/esp-idf-ci-action@v1
      with:
        esp_idf_version: v5.1.2
        target: esp32s3
        path: 'esp32/reloj_toques_espidf'

    - name: Upload firmware binaries
      uses: actions/upload-artifact@v4
      with:
        name: esp32s3-firmware
        path: |
          esp32/reloj_toques_espidf/build/*.bin
          esp32/reloj_toques_espidf/build/*.elf
          esp32/reloj_toques_espidf/build/bootloader/bootloader.bin
          esp32/reloj_toques_espidf/build/partition_table/partition-table.bin
